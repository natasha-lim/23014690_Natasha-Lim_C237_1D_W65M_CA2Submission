<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js"></script>
    <link rel="stylesheet" type="text/css" href="/css/styles.css">
    <title>Anne's Accessory Box</title>
</head>

<body>
    <%- include ('partials/navbar.ejs') %>
    <h1>Products</h1>
    <section class="container product-overview my-5 pt-5">
        <div class="row product-row">
            <!-- Image appearance on product section of website (single image, no need to add multiple for now)-->
            <div class="col-lg-5 col-md-12 col-12 image-col">
                <% for (let i=0; i < products.length; i++) { %>
                    <div class="product-item">
                        <img src="<%= products[i].image %>" alt="product image" class="img-fluid" style="width: 300px; height: 300px;"> <!-- product image-->
                    </div>
                <% } %>
            </div>
    
            <!-- Product Description goes here-->
            <div class="col-lg-5 col-md-12 col-12 image-col" >
                <% for (let i=0; i < products.length; i++) { %>
                    <div class="product-item">
                        <h2><%= products[i].name %></h2> 
                        <h4 class="mt-3 mb-5">Product Details</h4> 
                        <p><%= products[i].description %></p>
                        <p>Price: $<%= products[i].price %></p>
                    </div>
                <% } %>
            </div>
        </div> 
    </section>
</body>
</html>